# AI Visual Studio Portfolio

Static premium portfolio website built with Next.js, Tailwind CSS, Framer Motion and lucide-react.

## Run

```bash
npm install
npm run dev
```

Media files are expected in `public/assets/`.
